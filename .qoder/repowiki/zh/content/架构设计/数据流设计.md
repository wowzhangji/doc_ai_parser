# 数据流设计

<cite>
**本文引用的文件**
- [api.py](file://src/api.py)
- [cli.py](file://src/cli.py)
- [parser.py](file://src/core/parser.py)
- [chunker.py](file://src/core/chunker.py)
- [loader.py](file://src/core/loader.py)
- [merger.py](file://src/core/merger.py)
- [incremental.py](file://src/core/incremental.py)
- [document.py](file://src/models/document.py)
- [result.py](file://src/models/result.py)
- [request.py](file://src/models/request.py)
- [factory.py](file://src/providers/factory.py)
- [base.py](file://src/providers/base.py)
- [config.py](file://src/config.py)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考量](#性能考量)
8. [故障排查指南](#故障排查指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介
本文件面向API文档解析器的"数据流设计"，系统性描述从CLI/Web接口到核心引擎再到提供商适配层的完整数据处理流程。文档重点解释从用户输入到最终输出的端到端数据流向，涵盖文档加载、智能分块、LLM解析、结果合并与增量更新等关键步骤。文档详细说明数据在各组件间的传递方式、格式转换与验证机制，并提供异步处理的数据流控制与并发策略、错误处理与重试机制、数据一致性保障以及增量更新与缓存策略的设计说明。

## 项目结构
该项目采用"模块化+分层"的组织方式，实现了清晰的职责分离：
- **接口层**：CLI命令行接口和Web服务接口，负责用户交互和请求处理
- **核心处理层**：文档加载、智能分块、LLM解析、结果合并、增量更新
- **模型层**：统一的数据模型定义，确保跨组件的数据一致性
- **提供商适配层**：LLM提供商工厂与适配器，支持多种AI服务提供商
- **配置与工具层**：全局配置管理、日志记录、工具函数

```mermaid
graph TB
subgraph "接口层"
CLI["CLI命令行接口<br/>cli.py"]
API["Web服务接口<br/>api.py"]
end
subgraph "核心处理层"
Parser["LLM解析引擎<br/>core/parser.py"]
Chunker["智能分块器<br/>core/chunker.py"]
Loader["文档加载器<br/>core/loader.py"]
Merger["结果合并器<br/>core/merger.py"]
Incremental["增量解析器<br/>core/incremental.py"]
end
subgraph "模型层"
ModelsDoc["Document/Chunk/Section<br/>models/document.py"]
ModelsReq["ParseRequest/Config<br/>models/request.py"]
ModelsRes["ParseResult/Metadata<br/>models/result.py"]
end
subgraph "提供商适配层"
Factory["提供商工厂<br/>providers/factory.py"]
Base["提供商基类<br/>providers/base.py"]
end
subgraph "配置与工具层"
Config["配置管理<br/>config.py"]
Logger["日志记录<br/>structlog"]
end
CLI --> Parser
API --> Parser
Parser --> Loader
Parser --> Chunker
Parser --> Factory
Parser --> Merger
Parser --> Incremental
Loader --> ModelsDoc
Chunker --> ModelsDoc
Chunker --> ModelsRes
Parser --> ModelsReq
Parser --> ModelsRes
Merger --> ModelsRes
Incremental --> ModelsDoc
Incremental --> ModelsRes
Factory --> Base
API --> Config
CLI --> Config
```

**图表来源**
- [api.py](file://src/api.py#L24-L371)
- [cli.py](file://src/cli.py#L1-L393)
- [parser.py](file://src/core/parser.py#L20-L304)
- [loader.py](file://src/core/loader.py#L17-L328)
- [chunker.py](file://src/core/chunker.py#L10-L377)
- [merger.py](file://src/core/merger.py#L11-L220)
- [incremental.py](file://src/core/incremental.py#L14-L209)
- [document.py](file://src/models/document.py#L20-L75)
- [result.py](file://src/models/result.py#L8-L55)
- [request.py](file://src/models/request.py#L17-L57)
- [factory.py](file://src/providers/factory.py#L14-L71)
- [base.py](file://src/providers/base.py#L27-L143)
- [config.py](file://src/config.py#L7-L57)

## 核心组件
- **CLI接口**：命令行工具，支持本地文档解析、配置展示、示例生成等功能
- **Web接口**：FastAPI服务，提供异步任务处理、同步解析、提供商列表查询等REST API
- **LLM解析引擎**：核心处理单元，协调文档加载、分块、并发解析、结果合并
- **文档加载器**：支持PDF、Word、Excel、TXT/MD等多种格式，统一输出结构化Document对象
- **智能分块器**：基于结构感知与长度限制的分块策略，支持滑动窗口与重叠缓冲
- **提供商工厂**：抽象不同LLM提供商的接入，支持OpenAI/Azure/Anthropic/Ollama及自定义协议
- **结果合并器**：对多个ParseResult进行深度合并与去重，支持增量场景下的字段级合并
- **增量解析器**：基于文档/分块指纹检测变更，仅重新解析变更部分并合并历史结果
- **模型层**：统一的数据结构定义，确保跨组件的数据一致性

**章节来源**
- [cli.py](file://src/cli.py#L50-L393)
- [api.py](file://src/api.py#L76-L355)
- [parser.py](file://src/core/parser.py#L20-L304)
- [loader.py](file://src/core/loader.py#L17-L328)
- [chunker.py](file://src/core/chunker.py#L10-L377)
- [factory.py](file://src/providers/factory.py#L14-L71)
- [merger.py](file://src/core/merger.py#L11-L220)
- [incremental.py](file://src/core/incremental.py#L14-L209)
- [document.py](file://src/models/document.py#L20-L75)
- [result.py](file://src/models/result.py#L8-L55)
- [request.py](file://src/models/request.py#L17-L57)

## 架构总览
下图展示从CLI/Web接口到核心引擎再到提供商适配层的完整数据流，包括同步与异步两种模式、并发解析与结果合并、以及增量更新路径。

```mermaid
sequenceDiagram
participant User as "用户"
participant CLI as "CLI接口(cli.py)"
participant API as "Web接口(api.py)"
participant Parser as "解析引擎(parser.py)"
participant Loader as "加载器(loader.py)"
participant Chunker as "分块器(chunker.py)"
participant Factory as "提供商工厂(factory.py)"
participant Provider as "LLM提供商"
participant Merger as "合并器(merger.py)"
User->>CLI : "命令行参数"
User->>API : "HTTP请求"
CLI->>Parser : "构建ParseRequest并调用parse()"
API->>Parser : "异步/同步解析"
Parser->>Loader : "加载文档"
Loader-->>Parser : "返回Document"
Parser->>Chunker : "智能分块"
Chunker-->>Parser : "返回Chunk列表"
loop 并发解析每个Chunk
Parser->>Factory : "获取LLM提供商实例"
Factory-->>Parser : "LLMProvider实例"
Parser->>Provider : "parse(chunk, requirement, schema)"
Provider-->>Parser : "Chunk解析结果"
end
Parser->>Merger : "合并所有Chunk结果"
Merger-->>Parser : "返回ParseResult"
Parser-->>CLI/API : "返回最终结果"
```

**图表来源**
- [cli.py](file://src/cli.py#L127-L231)
- [api.py](file://src/api.py#L302-L355)
- [parser.py](file://src/core/parser.py#L46-L128)
- [loader.py](file://src/core/loader.py#L80-L328)
- [chunker.py](file://src/core/chunker.py#L28-L62)
- [factory.py](file://src/providers/factory.py#L14-L71)
- [merger.py](file://src/core/merger.py#L17-L79)

## 详细组件分析

### 组件A：CLI接口与Web接口
- **CLI接口**：命令行工具，支持本地文档解析、配置展示、示例生成等功能
- **Web接口**：FastAPI服务，提供异步任务处理、同步解析、提供商列表查询等REST API
- **输入**：CLI参数或HTTP请求体，包含文件、要求说明、输出Schema、配置参数
- **输出**：ParseResult或任务状态，支持进度回调和错误处理
- **关键能力**：
  - 文件类型检测、大小限制、Schema解析
  - 异步模式：内存任务池维护状态，后台任务执行解析
  - 同步模式：直接返回ParseResult
  - 提供商列表查询和配置展示

```mermaid
flowchart TD
Start(["开始"]) --> Detect["检测输入类型(CLI/Web)"]
Detect --> CLI{"CLI?"}
CLI --> |是| CLIParse["CLI解析流程"]
CLI --> |否| WebParse["Web解析流程"]
CLIParse --> BuildReq["构建ParseRequest"]
WebParse --> BuildReq
BuildReq --> Execute["执行解析"]
Execute --> Output["返回结果"]
```

**图表来源**
- [cli.py](file://src/cli.py#L127-L231)
- [api.py](file://src/api.py#L76-L255)

**章节来源**
- [cli.py](file://src/cli.py#L50-L393)
- [api.py](file://src/api.py#L76-L355)

### 组件B：文档加载与结构识别
- **输入**：用户上传的文件（PDF/Word/Excel/TXT/MD），或文件二进制内容
- **输出**：Document对象，包含content、metadata、structure（sections、headings、tables）
- **关键能力**：
  - 多格式解析：PDF使用pymupdf/pdfplumber；Word使用python-docx；Excel使用pandas/openpyxl；TXT/MD直接读取
  - API端点检测：正则匹配REST端点、URL、Endpoint声明等
  - 章节结构识别：标题层级、代码块、表格等
- **数据格式转换**：二进制→文本/结构化表格→Document
- **验证机制**：文件类型检测、大小限制、异常捕获

```mermaid
flowchart TD
Start(["开始"]) --> Detect["检测文件类型"]
Detect --> LoadPDF{"PDF?"}
LoadPDF --> |是| PDFLoad["使用pymupdf/pdfplumber加载"]
LoadPDF --> |否| LoadWord{"Word?"}
LoadWord --> |是| WordLoad["使用python-docx加载"]
LoadWord --> |否| LoadExcel{"Excel?"}
LoadExcel --> |是| ExcelLoad["使用pandas/openpyxl加载"]
LoadExcel --> |否| TextLoad["读取TXT/MD文本"]
PDFLoad --> Sections["检测API章节/标题/代码块"]
WordLoad --> Sections
ExcelLoad --> Sections
TextLoad --> Sections
Sections --> BuildDoc["构建Document对象"]
BuildDoc --> End(["结束"])
```

**图表来源**
- [loader.py](file://src/core/loader.py#L80-L328)

**章节来源**
- [loader.py](file://src/core/loader.py#L17-L328)

### 组件C：智能分块与上下文增强
- **输入**：Document（含content与structure）
- **输出**：Chunk列表，每个Chunk包含content、index、metadata、sections、context
- **关键策略**：
  - 语义分块：按标题、API端点、表格/代码块等结构边界切分
  - 长度限制：按token估算（1token≈4字符）控制每块大小
  - 滑动窗口：超长块按句子边界细分，保留重叠缓冲
  - 上下文注入：为每个Chunk附加全局信息与邻近Chunk摘要
- **数据格式转换**：Document→Chunk[]
- **验证机制**：token估算、边界检测、重叠一致性

```mermaid
flowchart TD
S(["开始"]) --> HasStruct{"有结构信息?"}
HasStruct --> |否| ByText["按段落/文本分块"]
HasStruct --> |是| Semantic["语义分块(标题/API端点/表格/代码)"]
Semantic --> Large{"块过大?"}
Large --> |是| Slide["滑动窗口细分(保留重叠)"]
Large --> |否| Keep["保留原块"]
Slide --> AddCtx["添加上下文(全局+邻居摘要)"]
Keep --> AddCtx
ByText --> Large2{"块过大?"}
Large2 --> |是| Slide2["滑动窗口细分"]
Large2 --> |否| AddCtx2["添加上下文"]
Slide2 --> AddCtx2
AddCtx --> E(["结束"])
AddCtx2 --> E
```

**图表来源**
- [chunker.py](file://src/core/chunker.py#L28-L377)

**章节来源**
- [chunker.py](file://src/core/chunker.py#L10-L377)

### 组件D：LLM解析与并发控制
- **输入**：ParseRequest（包含DocumentSource、RequirementDoc、ParseConfig）
- **输出**：ParseResult（data、metadata、source_fingerprint）
- **关键流程**：
  - 文档指纹计算（content SHA256前16位）
  - 并发解析：信号量限制并发数，支持进度回调
  - 缓存策略：基于chunk+requirement+model的缓存键
  - 错误处理：异常转为内部错误标记，不影响整体流程
  - 结果合并：深度合并字典、列表去重合并
- **数据格式转换**：Chunk[]→ParseResult
- **验证机制**：置信度计算、警告收集、处理时间统计

```mermaid
sequenceDiagram
participant Parser as "LLMParser"
participant Loader as "Loader"
participant Chunker as "SmartChunker"
participant Factory as "LLMProvider"
participant Merger as "Merger"
Parser->>Loader : "加载文档"
Loader-->>Parser : "Document"
Parser->>Parser : "计算source_fingerprint"
Parser->>Chunker : "分块"
Chunker-->>Parser : "Chunks"
loop 并发解析
Parser->>Factory : "parse(chunk, requirement, schema)"
Factory-->>Parser : "chunk_result"
end
Parser->>Merger : "合并结果"
Merger-->>Parser : "ParseResult"
Parser-->>Parser : "构造metadata(置信度/警告/耗时)"
```

**图表来源**
- [parser.py](file://src/core/parser.py#L46-L128)
- [factory.py](file://src/providers/factory.py#L14-L71)
- [merger.py](file://src/core/merger.py#L17-L79)

**章节来源**
- [parser.py](file://src/core/parser.py#L20-L304)
- [request.py](file://src/models/request.py#L51-L57)
- [result.py](file://src/models/result.py#L20-L55)

### 组件E：提供商适配层
- **输入**：Chunk、RequirementDoc、OutputSchema
- **输出**：解析结果字典，包含结构化数据和警告信息
- **关键能力**：
  - 工厂模式：根据provider_name返回对应LLMProvider实例
  - 统一接口：所有提供商实现相同的parse方法
  - 配置管理：支持API Key、Base URL、模型名称、温度参数
  - JSON解析：自动提取和验证JSON响应
- **支持的提供商**：OpenAI、Azure OpenAI、Anthropic、Custom OpenAI、Custom Anthropic、Ollama

```mermaid
flowchart TD
Start(["开始"]) --> GetProvider["get_provider(provider_name)"]
GetProvider --> Check{"提供商类型?"}
Check --> |OpenAI| OpenAI["OpenAIProvider"]
Check --> |Azure| Azure["AzureOpenAIProvider"]
Check --> |Anthropic| Anthropic["AnthropicProvider"]
Check --> |Custom OpenAI| CustomOpenAI["CustomOpenAIProvider"]
Check --> |Custom Anthropic| CustomAnthropic["CustomAnthropicProvider"]
Check --> |Ollama| Ollama["OllamaProvider"]
OpenAI --> Instance["返回Provider实例"]
Azure --> Instance
Anthropic --> Instance
CustomOpenAI --> Instance
CustomAnthropic --> Instance
Ollama --> Instance
```

**图表来源**
- [factory.py](file://src/providers/factory.py#L14-L71)
- [base.py](file://src/providers/base.py#L27-L143)

**章节来源**
- [factory.py](file://src/providers/factory.py#L14-L71)
- [base.py](file://src/providers/base.py#L27-L143)

### 组件F：结果合并与去重
- **输入**：多个ParseResult（或增量更新产生的新结果）
- **输出**：合并后的ParseResult
- **关键策略**：
  - 深度合并字典、列表去重合并
  - 基于关键字段（path/method/name/url/id等）对API端点去重
  - 合并元数据：总块数、已处理块数、失败块索引、置信度、警告、处理时间
- **数据格式转换**：List[ParseResult]→ParseResult
- **验证机制**：字段唯一性检查、集合去重

```mermaid
flowchart TD
S(["开始"]) --> MergeData["深度合并data字典"]
MergeData --> MergeLists["智能合并列表(去重)"]
MergeLists --> DedupEP["API端点去重(path+method)"]
DedupEP --> MergeMeta["合并元数据(统计/警告/耗时)"]
MergeMeta --> E(["结束"])
```

**图表来源**
- [merger.py](file://src/core/merger.py#L17-L220)

**章节来源**
- [merger.py](file://src/core/merger.py#L11-L220)

### 组件G：增量更新与缓存
- **输入**：旧Document、新Document、旧ParseResult、新Chunk解析结果
- **输出**：增量合并后的ParseResult
- **关键策略**：
  - 文档指纹与分块指纹对比，识别变更/未变更块
  - 未变更块保留旧结果，变更块重新解析后合并
  - 可选全量重解析阈值判断（基于文档大小差异比例）
  - 缓存：内存缓存（可扩展为Redis等持久化缓存）
- **数据格式转换**：旧结果+新结果→合并结果
- **验证机制**：指纹集合、变更比例阈值

```mermaid
flowchart TD
S(["开始"]) --> NewChunks["对新Document分块"]
NewChunks --> FP["计算新Chunk指纹"]
FP --> Compare{"与旧指纹集合比较"}
Compare --> |命中| Unchanged["标记未变更块"]
Compare --> |未命中| Changed["标记变更块"]
Changed --> ReParse["并发解析变更块"]
Unchanged --> Merge["合并旧结果+新结果"]
ReParse --> Merge
Merge --> Meta["更新元数据(置信度/警告/耗时)"]
Meta --> E(["结束"])
```

**图表来源**
- [incremental.py](file://src/core/incremental.py#L29-L151)

**章节来源**
- [incremental.py](file://src/core/incremental.py#L14-L209)

## 依赖关系分析
- **组件耦合与内聚**：
  - LLMParser与SmartChunker强耦合（分块策略由其决定）
  - LLMParser与LLMProvider弱耦合（通过工厂解耦）
  - Loader与Document模型强耦合（输出统一结构）
  - Merger与Result模型强耦合（合并策略依赖结构）
  - IncrementalParser与Document/Chunk/Result模型强耦合（指纹与结构）
- **外部依赖**：
  - 文档处理：pymupdf、pdfplumber、python-docx、openpyxl、pandas
  - LLM SDK：openai、anthropic
  - Web框架：fastapi、uvicorn
  - CLI框架：typer、rich
  - 日志：structlog

```mermaid
graph LR
CLI["cli.py"] --> Parser["core/parser.py"]
API["api.py"] --> Parser
Parser --> Chunker["core/chunker.py"]
Parser --> Loader["core/loader.py"]
Parser --> Factory["providers/factory.py"]
Parser --> Merger["core/merger.py"]
Parser --> Incremental["core/incremental.py"]
Loader --> Doc["models/document.py"]
Chunker --> Doc
Merger --> Res["models/result.py"]
Incremental --> Doc
Incremental --> Res
Factory --> Base["providers/base.py"]
API --> Config["config.py"]
CLI --> Config
```

**图表来源**
- [cli.py](file://src/cli.py#L23-L393)
- [api.py](file://src/api.py#L21-L371)
- [parser.py](file://src/core/parser.py#L14-L304)
- [chunker.py](file://src/core/chunker.py#L10-L377)
- [loader.py](file://src/core/loader.py#L17-L328)
- [merger.py](file://src/core/merger.py#L11-L220)
- [incremental.py](file://src/core/incremental.py#L14-L209)
- [document.py](file://src/models/document.py#L20-L75)
- [result.py](file://src/models/result.py#L8-L55)
- [factory.py](file://src/providers/factory.py#L14-L71)
- [base.py](file://src/providers/base.py#L27-L143)
- [config.py](file://src/config.py#L7-L57)

**章节来源**
- [config.py](file://src/config.py#L7-L57)

## 性能考量
- **并发解析**：通过信号量限制并发数（默认5个），避免LLM提供商限流或资源争用
- **缓存策略**：基于chunk+requirement+model的缓存键，减少重复请求
- **分块策略**：语义分块+滑动窗口+重叠缓冲，兼顾吞吐与信息完整性
- **异步任务**：后台任务池避免阻塞主线程，支持进度回调
- **增量更新**：指纹对比仅重解析变更块，显著降低处理成本
- **内存管理**：任务完成后清理文件内容，避免内存泄漏

## 故障排查指南
- **常见问题与定位**：
  - 文件类型不支持：检查文件后缀映射与支持列表
  - 文件过大：检查max_file_size配置
  - JSON Schema无效：检查output_schema的JSON格式
  - LLM提供商配置错误：检查api_key/api_base/model/provider
  - 并发异常：查看信号量限制与异常处理日志
- **日志与监控**：
  - 使用structlog记录关键事件（document_loaded、document_chunked、parse_completed、chunk_parse_failed等）
  - 通过TaskStatus的progress字段观察解析进度
- **重试与容错**：
  - LLMProvider层具备最大重试次数配置
  - 解析失败的Chunk会标记错误并继续处理其他Chunk，保证整体可用性

**章节来源**
- [api.py](file://src/api.py#L108-L124)
- [parser.py](file://src/core/parser.py#L130-L201)

## 结论
本数据流设计围绕"CLI/Web接口→核心引擎→提供商适配层"的清晰分层架构展开，通过统一的数据模型与工厂化提供商接入，实现了从多格式文档到结构化结果的高可靠、高性能处理。模块化的架构设计使得各组件职责明确、耦合度低，便于维护和扩展。异步任务与缓存策略进一步提升了用户体验与系统吞吐。建议在生产环境中引入持久化缓存（如Redis）与任务队列（Celery+Redis），并结合监控与告警体系完善可观测性。

## 附录

### 数据模型概览
```mermaid
classDiagram
class Document {
+string content
+dict metadata
+DocumentStructure structure
+string file_type
+string file_path
}
class DocumentStructure {
+DocumentSection[] sections
+dict[] headings
+dict[] tables
+get_api_sections() DocumentSection[]
}
class DocumentSection {
+SectionType type
+string content
+int level
+dict metadata
+DocumentSection[] children
}
class Chunk {
+string content
+int index
+dict metadata
+DocumentSection[] sections
+string context
+estimate_tokens() int
}
class ParseRequest {
+DocumentSource source_document
+RequirementDoc requirement_doc
+ParseConfig config
+dict previous_result
}
class ParseConfig {
+string provider
+string model
+string api_base
+string api_key
+int chunk_size
+int chunk_overlap
+float temperature
+int max_retries
+bool use_cache
}
class RequirementDoc {
+string content
+dict output_schema
+ExtractionRule[] extraction_rules
}
class ParseResult {
+string version
+datetime parsed_at
+string source_fingerprint
+dict data
+ParseMetadata metadata
+bool is_incremental
+string[] changed_fields
}
class ParseMetadata {
+int total_chunks
+int processed_chunks
+int[] failed_chunks
+float confidence_score
+string[] warnings
+float processing_time
+string model_used
+string provider_used
}
Document --> DocumentStructure
DocumentStructure --> DocumentSection
Chunk --> DocumentSection
ParseRequest --> DocumentSource
ParseRequest --> RequirementDoc
ParseRequest --> ParseConfig
ParseResult --> ParseMetadata
```

**图表来源**
- [document.py](file://src/models/document.py#L20-L75)
- [result.py](file://src/models/result.py#L8-L55)
- [request.py](file://src/models/request.py#L17-L57)

### 状态转换图（任务生命周期）
```mermaid
stateDiagram-v2
[*] --> 待处理
待处理 --> 处理中 : "创建任务"
处理中 --> 已完成 : "解析成功"
处理中 --> 失败 : "解析异常"
已完成 --> [*]
失败 --> [*]
```

**图表来源**
- [api.py](file://src/api.py#L302-L355)

### 提供商支持矩阵
```mermaid
graph LR
subgraph "官方提供商"
OpenAI["OpenAI<br/>官方API"]
Azure["Azure OpenAI<br/>官方API"]
Anthropic["Anthropic Claude<br/>官方API"]
end
subgraph "自定义提供商"
CustomOpenAI["Custom OpenAI<br/>vLLM/TGI等"]
CustomAnthropic["Custom Anthropic<br/>自定义协议"]
Ollama["Ollama<br/>本地模型"]
end
OpenAI --> Provider["LLMProvider接口"]
Azure --> Provider
Anthropic --> Provider
CustomOpenAI --> Provider
CustomAnthropic --> Provider
Ollama --> Provider
```

**图表来源**
- [factory.py](file://src/providers/factory.py#L50-L71)
- [base.py](file://src/providers/base.py#L27-L143)